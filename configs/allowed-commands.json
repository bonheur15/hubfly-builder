{
  "prebuild": [
    "npm install",
    "npm ci",
    "yarn install",
    "pip install -r requirements.txt",
    "composer install",
    "go mod download",
    "bun install"
  ],
  "build": [
    "npm run build",
    "yarn build",
    "go build ./...",
    "bun build",
    "bun run build",
    "python setup.py build"
  ],
  "run": [
    "npm start",
    "yarn start",
    "go run main.go",
    "python main.py",
    "bun run start",
    "node server.js"
  ]
}
